//! 此文件由 build.rs 自动生成，请勿手动修改
//! Auto-generated by build.rs, DO NOT EDIT
//!
//! 独立程序入口: basic - Basic utils tools (URL, String, Bin, XOR, Random)

use clap::Parser;
use enyo::{cmds::basic::Cmd, CmdExecute};

#[tokio::main]
async fn main() {
    match Cmd::try_parse() {
        Ok(cmd) => {
            if let Err(e) = cmd.execute().await {
                eprintln!("{e}");
                std::process::exit(1);
            }
        }
        Err(e) => {
            eprintln!("{e}");
            std::process::exit(1);
        }
    }
}
